from __future__ import annotations

from collections.abc import Iterable
from typing import Any


WELCOME_START = """Добро пожаловать в ЭРА.

Здесь активность не теряется. Каждая встреча, задача, проект и идея собираются в Ваш путь: баллы, портфолио, рейтинг и новые возможности.

Начните с регистрации — откроем личный кабинет и подберём первый шаг."""

MAIN_MENU = """Главное меню ЭРА

Выберите раздел. Здесь всё связано: участие даёт опыт, опыт попадает в портфолио, баллы двигают рейтинг и открывают возможности."""

MAIN_INLINE_MENU = """Главное меню ЭРА

Куда идём дальше? Можно начать с афиши, задач, возможностей или личного кабинета."""

CABINET_EMPTY_HINT = """Путь только начинается.

Сейчас кабинет выглядит спокойно, но он быстро оживает: одно мероприятие, одна задача, один проект — и у Вас уже есть история роста."""

EVENTS_LIST_HEADER = """Афиша ЭРА

Выберите событие. Участие попадёт в Ваш путь, баллы и портфолио."""

EVENTS_EMPTY = """Афиша пока пустая.

Это не пауза — команда готовит новые встречи, игры и форматы. Пока можно открыть задачи, выбрать направление или собрать идею проекта."""

TASKS_MENU = """Мои задачи

Задачи — самый быстрый способ перейти от участника к активному. Выберите, что посмотреть."""

TASKS_EMPTY_ACTIVE = """Активные задачи скоро появятся.

Хороший момент выбрать мероприятие, зайти в направление или предложить свою идею. Как только появится задача — она будет здесь."""

TASKS_EMPTY_ARCHIVE = """Архив задач пока пустой.

Здесь появятся завершённые результаты: то, что Вы сделали, сдали и добавили в историю роста ЭРА."""

OPPORTUNITIES_MENU = """Возможности

Ваш баланс: {balance} баллов.

Баллы — это след Вашей активности. Их можно обменивать на партнёрские предложения, награды, встречи и специальные форматы ЭРА."""

REWARDS_MENU = """Возможности за баллы

Ваш баланс: {balance} баллов.

Каталог показывает, во что превращается активность: награды, партнёрские предложения и специальные форматы."""

REWARDS_EMPTY = """Каталог обновляется.

Продолжайте участвовать, брать задачи и копить баллы. Когда появится сильное предложение от партнёров, Вы увидите его здесь."""

CONTACT_MENU = """Связь

Здесь можно задать вопрос, найти команду, открыть правила или перейти к департаментам. Не зависайте в одиночку — ЭРА строится через людей."""

CONTACT_CALLBACK = """Связь

Выберите, что нужно: вопрос, команда, правила или информация о боте."""

TEAM_MENU = """Команда ЭРА

Здесь видно, кто ведёт направления, где чаты и куда подключаться, чтобы не просто наблюдать, а войти в движение."""

ANALYTICS_EMPTY = """Аналитика пока пустая.

Система уже готова считать участников, события, задачи, цели и партнёров. Как только начнётся движение в боте, здесь появится картина роста ЭРА."""


def profile_missing_fields(user: Any) -> list[str]:
    checks = (
        ("возраст", getattr(user, "age", None)),
        ("город", getattr(user, "city", None)),
        ("учёба / работа", getattr(user, "education_work", None)),
        ("email", getattr(user, "email", None)),
        ("телефон", getattr(user, "phone", None)),
    )
    return [label for label, value in checks if not value]


def profile_empty_hint(user: Any) -> str:
    missing = profile_missing_fields(user)
    if not missing:
        return ""
    visible = ", ".join(missing[:3])
    tail = " и ещё" if len(missing) > 3 else ""
    return (
        "\n\nПрофиль ещё можно усилить: "
        f"{visible}{tail}. Чем точнее данные, тем легче собрать портфолио, документы и подходящие возможности."
    )


def journey_intro(name: str, has_activity: bool) -> str:
    if has_activity:
        return f"{name}, здесь уже виден Ваш путь: участие, задачи, проекты, баллы и достижения."
    return f"{name}, путь только начинается. Первое действие в ЭРА уже станет частью Вашей истории роста."


def rating_text(rows: Iterable[tuple[str, int]], current_place: int | str, points: int) -> str:
    top = "\n".join(
        f"{index}. {name} — {score}"
        for index, (name, score) in enumerate(rows, 1)
    )
    if not top:
        top = "Рейтинг пока пустой. Он оживёт после первых подтверждённых действий участников."
    return f"""Рейтинг ЭРА

Ваше место: {current_place}
Ваши баллы: {points}

Топ участников:
{top}

Рейтинг — не гонка за цифрой. Он показывает тех, кто двигается, помогает и берёт ответственность."""


def analytics_summary(
    *,
    users: int,
    approved: int,
    pending: int,
    events: int,
    projects: int,
    contacts: int,
    goals: int,
) -> str:
    base = (
        "Аналитика ЭРА\n\n"
        f"Участников в базе: {users}\n"
        f"Одобрены: {approved}\n"
        f"Новые заявки: {pending}\n"
        f"Мероприятий: {events}\n"
        f"Проектов: {projects}\n"
        f"Организаций в базе: {contacts}\n"
        f"Целей месяца: {goals}"
    )
    if not any((users, events, projects, contacts, goals)):
        return f"{base}\n\n{ANALYTICS_EMPTY}"
    return f"{base}\n\nМожно скачать всю книгу или только нужный раздел."
